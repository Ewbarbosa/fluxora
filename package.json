{
  "name": "fluxora",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "preinstall": "node ./scripts/require-pnpm.cjs",
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "build:web": "pnpm --filter @fluxora/web build",
    "build:api": "pnpm --filter @fluxora/api build",
    "start:web": "pnpm --filter @fluxora/web start",
    "start:api": "pnpm --filter @fluxora/api start:prod",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "format": "turbo run format"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  }
}
